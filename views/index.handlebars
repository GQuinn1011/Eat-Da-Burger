<div class="jumbotron">
  <h1 class="display-4">Burger Heaven!</h1>
  <p>Add a burger and/or choose one to be devoured!</p>
</div>

<div class="row">
	<div class="col-sm-4 col-sm-offset-1">
    <div class="card">
  <div class="card-header">
    <h2 class="card-title">Burgers to eat:</h2>
    <ul>
      {{#each burgers}}
      {{#unless this.devoured}}
        <li>
          {{this.id}}.{{this.burger_name}}
          <form action="/{{this.id}}?_method=PUT" method="POST">
          <input type="hidden" name="devoured" value="false">
          <button class="btn btn-danger" type="submit">Devour It!</button>
          </form>
        </li>
        {{/unless}}{{/each}}
    </ul>
    </div>
  </div>
  </div>


    <div class="col-sm-4 col-sm-offset-1">
      <div class="card">
        <div class="card-header">
        <h2 class="card-title">Devoured Burgers:</h2>

          <ul>
          {{#each burgers}}
          {{#if this.devoured}}
          <li>
          {{this.id}}.{{this.burger_name}}
          </form>
          </li>
          {{/if}}{{/each}}
          </ul>
        </div>
        </div>
      </div>
      </div>

      <div class="row">
	      <div class="col-sm-4 col-sm-offset-4">
          <div class="card">
            <div id="add" class="card-header">
            <h2 class="card-title">Add a Burger</h2>
          <form class="create-update-form" action="/" method="POST">

        <div class="form-group">
          <label for="ca">Burger Name:</label>
          <input type="text" id="ca" name="burger_name">
        </div>

        <button class="btn btn-success" type="submit">Add Burger</button>
          </form>
            </div>
          </div>
          </div>
	
  	    </div>
</div>